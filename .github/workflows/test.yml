name: Demo
on: 
    push:
        branches: [main]
        
jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: Install Dependencies
              run:  npm install
            - name: Code Deploy
              run:  echo 'DEploying......'    




